import {createContext,useContext,useEffect,useState} from 'react'; import {onAuthStateChanged,signOut} from 'firebase/auth'; import {auth} from '../services/firebase'; import {api} from '../api/client'; import type {User} from '../types';
const C=createContext<{user:User|null;loading:boolean;logout:()=>Promise<void>}>({user:null,loading:true,logout:async()=>{}}); export function AuthProvider({children}:{children:React.ReactNode}){const [user,setUser]=useState<User|null>(null);const[loading,setLoading]=useState(true);useEffect(()=>onAuthStateChanged(auth,async u=>{if(!u){setUser(null);setLoading(false);return}try{const r=await api<{user:User}>('/auth/me');setUser(r.user)}catch{await signOut(auth);setUser(null)}finally{setLoading(false)}}),[]);return <C.Provider value={{user,loading,logout:()=>signOut(auth)}}>{children}</C.Provider>} export const useAuth=()=>useContext(C);