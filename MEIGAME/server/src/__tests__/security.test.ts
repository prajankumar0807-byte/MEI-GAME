import{describe,it,expect}from'vitest';import request from'supertest';import app from'../app.js';describe('security smoke',()=>{it('requires auth for protected users route',async()=>{const r=await request(app).get('/api/users');expect(r.status).toBe(401)});it('health is public',async()=>{const r=await request(app).get('/api/health');expect(r.status).toBe(200);expect(r.body.ok).toBe(true)})})