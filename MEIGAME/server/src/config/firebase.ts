import admin from 'firebase-admin'; import 'dotenv/config'; let app:admin.app.App; export function getFirebase(){if(app)return app; const privateKey=(process.env.FIREBASE_PRIVATE_KEY||'').replace(/\\n/g,'\n'); if(!process.env.FIREBASE_PROJECT_ID||!process.env.FIREBASE_CLIENT_EMAIL||!privateKey){throw new Error('Firebase Admin environment is not configured.');} app=admin.initializeApp({credential:admin.credential.cert({projectId:process.env.FIREBASE_PROJECT_ID,clientEmail:process.env.FIREBASE_CLIENT_EMAIL,privateKey}),storageBucket:process.env.FIREBASE_STORAGE_BUCKET});return app;} export function db(){return getFirebase().firestore()} export function authAdmin(){return getFirebase().auth()}