import {FormEvent,useState} from 'react'; import {useNavigate} from 'react-router-dom'; import Logo from '../components/Logo'; import {useAuth} from '../contexts/AuthContext';
export default function Login(){const [email,setEmail]=useState(''),[password,setPassword]=useState(''),[err,setErr]=useState('');const {login}=useAuth();const nav=useNavigate();async function submit(e:FormEvent){e.preventDefault();setErr('');try{await login(email.includes('@')?email:`${email}@mame.local`,password);nav('/admin')}catch(e){setErr(e instanceof Error?e.message:'Login failed')}}return <div className="min-h-screen grid lg:grid-cols-2 grid-bg"><section className="hidden lg:flex p-14 flex-col justify-between"><Logo/><div><div className="pill inline-block mb-5">PRIVATE COLLEGE QUIZ PLATFORM</div><h1 className="text-6xl font-black leading-[.95] max-w-xl">Run live quizzes with <span className="text-transparent bg-clip-text bg-gradient-to-r from-violet-400 to-cyan-300">control.</span></h1><p className="muted text-lg mt-6 max-w-lg">Secure scoring, role-based administration, live sessions and results built for real college events.</p></div><div className="muted text-sm">MEIGAME • Production-ready architecture</div></section><section className="grid place-items-center p-6"><form onSubmit={submit} className="glass card3d rounded-3xl p-8 w-full max-w-md"><div className="mb-7"><Logo/><h2 className="text-2xl font-bold mt-8">Welcome back</h2><p className="muted text-sm mt-1">Sign in with your Firebase account.</p></div>{err&&<div className="mb-4 rounded-xl border border-red-400/20 bg-red-500/10 p-3 text-sm text-red-200">{err}</div>}<label className="block text-sm mb-2">Username<input className="input mt-2" type="text" required value={email} onChange={e=>setEmail(e.target.value)} /></label><label className="block text-sm mt-4 mb-2">Password<input className="input mt-2" type="password" required value={password} onChange={e=>setPassword(e.target.value)} /></label><button className="btn btn-primary w-full mt-6">Sign in</button><p className="muted text-xs mt-5">Development accounts are created by the Firebase seed script. Passwords are never stored in Firestore.</p></form></section></div>}
