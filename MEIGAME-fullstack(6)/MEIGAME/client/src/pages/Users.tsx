import {useQuery} from '@tanstack/react-query';import {api} from '../api/http';import type {User} from '../types';export default function Users(){const q=useQuery({queryKey:['users'],queryFn:()=>api<{items:User[]}>('/users')});return <div><h1 className="text-4xl font-black">Users</h1><p className="muted mt-2 mb-6">Registered college accounts.</p><div className="glass rounded-2xl overflow-hidden"><div className="overflow-auto"><table className="w-full text-sm"><thead className="bg-white/5"><tr><th className="text-left p-4">Name</th><th className="text-left p-4">Username</th><th className="text-left p-4">Role</th><th className="text-left p-4">Status</th></tr></thead><tbody>{q.data?.items.map(u=><tr className="border-t border-white/5" key={u.uid}><td className="p-4 font-medium">{u.fullName}</td><td className="p-4 muted">{u.username}</td><td className="p-4"><span className="pill">{u.role}</span></td><td className="p-4">{u.isActive?'Active':'Disabled'}</td></tr>)}</tbody></table></div></div></div>}
