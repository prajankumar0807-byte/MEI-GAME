import {useEffect,useState} from 'react';import {useNavigate,useParams} from 'react-router-dom';import {api} from '../api/http';export default function ParticipantComplete(){const {sessionId}=useParams();const [done,setDone]=useState(false);const nav=useNavigate();useEffect(()=>{api(`/participant/session/${sessionId}/complete`,{method:'POST'}).then(()=>setDone(true)).catch(()=>setDone(true))},[sessionId]);return <div className="min-h-screen grid place-items-center p-6"><div className="glass rounded-3xl p-10 text-center max-w-md"><div className="text-6xl">✓</div><h1 className="text-4xl font-black mt-4">Quiz complete</h1><p className="muted mt-3">Your answers were scored on the server.</p>{done&&<button className="btn btn-primary mt-6" onClick={()=>nav(`/participant/session/${sessionId}/result`)}>View result</button>}</div></div>}
