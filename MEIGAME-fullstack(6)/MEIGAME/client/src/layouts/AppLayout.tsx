import {Link,Outlet,useLocation} from 'react-router-dom'; import {BarChart3,ClipboardList,LayoutDashboard,LogOut,Settings,Users,UserCog,Activity} from 'lucide-react'; import Logo from '../components/Logo'; import {useAuth} from '../contexts/AuthContext';
export default function AppLayout(){const {user,logout}=useAuth();const loc=useLocation();const items=[['/admin','Dashboard',LayoutDashboard],['/admin/quizzes','Quizzes',ClipboardList],['/admin/users','Users',Users],['/admin/staff','Staff',UserCog],['/admin/analytics','Analytics',BarChart3],['/admin/activity','Activity Logs',Activity],['/admin/settings','Settings',Settings]] as const;return <div className="min-h-screen flex"><aside className="hidden md:flex w-64 p-5 flex-col gap-7 border-r border-white/10 bg-black/20"><Logo/><nav className="space-y-2">{items.map(([to,label,Icon])=><Link key={to} to={to} className={`flex items-center gap-3 p-3 rounded-xl ${loc.pathname===to?'bg-white/10':'hover:bg-white/5'}`}><Icon size={18}/>{label}</Link>)}</nav><div className="mt-auto"><div className="glass rounded-2xl p-4 mb-3"><div className="font-bold">{user?.fullName||user?.username}</div><div className="muted text-xs mt-1">{user?.role}</div></div><button onClick={logout} className="btn btn-ghost w-full gap-2"><LogOut size={16}/>Sign out</button></div></aside><main className="flex-1 min-w-0"><div className="md:hidden p-4 border-b border-white/10"><Logo/></div><div className="p-4 md:p-8 max-w-7xl mx-auto"><Outlet/></div></main></div>}
