import {z} from 'zod';export const quizSchema=z.object({title:z.string().trim().min(2).max(160),description:z.string().max(2000).optional(),subject:z.string().max(100).optional(),department:z.string().max(100).optional(),year:z.string().max(20).optional(),difficulty:z.string().max(30).optional(),status:z.enum(['DRAFT','PUBLISHED','LIVE','ENDED','ARCHIVED']).optional(),timeLimit:z.number().int().min(1).max(86400).optional(),leaderboardEnabled:z.boolean().optional()});export const joinSchema=z.object({name:z.string().trim().min(2).max(80),collegeId:z.string().trim().min(1).max(80)});export const answerSchema=z.object({questionId:z.string().min(1),selectedOptionId:z.string().min(1)});export const userSchema=z.object({fullName:z.string().min(2).max(100),username:z.string().min(2).max(50),email:z.string().email(),collegeId:z.string().optional(),department:z.string().optional(),year:z.string().optional(),section:z.string().optional(),role:z.enum(['SUPER_ADMIN','STAFF','USER']).default('USER')});
