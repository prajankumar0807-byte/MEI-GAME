import 'dotenv/config';import {adminAuth,db,FieldValue} from '../config/firebase.js';
const accounts=[{email:'mecprajan@mame.local',username:'mecprajan',fullName:'MECPrajan'},{email:'mecraju@mame.local',username:'mecraju',fullName:'MECRaju'}];const password='mahendra@123';
for(const a of accounts){let u;try{u=await adminAuth.getUserByEmail(a.email);await adminAuth.updateUser(u.uid,{password});}catch{u=await adminAuth.createUser({email:a.email,password,displayName:a.fullName,emailVerified:false})}await db.doc(`users/${u.uid}`).set({uid:u.uid,email:a.email,username:a.username,fullName:a.fullName,role:'SUPER_ADMIN',isActive:true,createdAt:FieldValue.serverTimestamp(),updatedAt:FieldValue.serverTimestamp()},{merge:true});await adminAuth.setCustomUserClaims(u.uid,{role:'SUPER_ADMIN'});console.log(`Seeded ${a.username} (${a.email})`)}
