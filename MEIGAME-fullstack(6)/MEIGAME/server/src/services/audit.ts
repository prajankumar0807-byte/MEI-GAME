import {db,FieldValue} from '../config/firebase.js';import type {AuthRequest} from '../types/index.js';export async function audit(req:AuthRequest,action:string,targetType:string,targetId:string,metadata:Record<string,unknown>={}){await db.collection('activityLogs').add({userId:req.user?.uid||null,action,targetType,targetId,metadata,ipAddress:req.ip,userAgent:req.get('user-agent')||'',createdAt:FieldValue.serverTimestamp()})}
